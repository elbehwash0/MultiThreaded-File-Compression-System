# MultiThreaded-File-Compression-System
A multi-threaded client-server application developed in C# .  The system allows multiple clients to upload files to a server,  where files are compressed into ZIP format and sent back to the client through binary data transfer.  The client application includes a Windows Forms GUI for selecting, sending, and receiving compressed files easily.
